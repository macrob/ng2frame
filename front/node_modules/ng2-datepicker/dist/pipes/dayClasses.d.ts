import { PipeTransform } from '@angular/core';
import { CalendarDay } from '../models';
export declare class DayClassesPipe implements PipeTransform {
    transform(day: CalendarDay): {
        [name: string]: boolean;
    };
}
