import { OnChanges, SimpleChanges } from '@angular/core';
import * as moment from 'moment';
import { BaseSelect } from './base.select';
export interface RangeDate {
    start: moment.Moment;
    end: moment.Moment;
}
export declare class RangeSelectDirective extends BaseSelect<RangeDate> implements OnChanges {
    protected readonly EMPTY_VALUE: RangeDate;
    setValue(value: RangeDate): void;
    ngOnChanges(changes: SimpleChanges): void;
    selectDate(date: moment.Moment): boolean;
    unselectDate(date: moment.Moment): boolean;
    isDateSelected(date: moment.Moment): boolean;
    isDateInSelectRange(date: moment.Moment): boolean;
}
