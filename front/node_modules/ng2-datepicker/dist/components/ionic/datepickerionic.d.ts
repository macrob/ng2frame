import { Component, ElementRef, OnInit, Renderer, ChangeDetectorRef } from '@angular/core';
import { DatePickerTemplate } from '../datepicker.template';
import { BaseSelect } from '../../selections/base.select';
import { CalendarDay } from '../../models';
export declare class DatePickerIonicComponent extends DatePickerTemplate<BaseSelect<any>, any> implements OnInit {
    static extendConfig(config: Component, componentClass: Function, ...a: any[]): Component;
    static CONTAINER_CLASS: string;
    private _class;
    class: string;
    private _opened;
    opened: boolean;
    expanded: boolean;
    viewFormat: string;
    private el;
    constructor(renderer: Renderer, elRef: ElementRef, cd: ChangeDetectorRef, select: BaseSelect<any>);
    ngOnInit(): void;
    onDateclick(e: MouseEvent, day: CalendarDay): void;
    nextMonth(): void;
    prevMonth(): void;
    toggle(): void;
    open(): void;
    close(): void;
    inputClick(): void;
}
