import { Moment } from 'moment';
export declare enum DateState {
    disabled = 0,
    enabled = 1,
    inRange = 2,
    selected = 3,
}
export interface CalendarDay {
    date: Moment;
    state: DateState;
    isToday: boolean;
    isCurrDisplayMonth: boolean;
}
